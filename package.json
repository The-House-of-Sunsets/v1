{
  "name": "exp",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "client-install": "yarn install --prefix client",
    "start": "node ./bin/www",
    "server": "nodemon app.js",    
    "client": "yarn start --prefix client",
    "dev": "concurrently \"yarn start --prefix client\" \"yarn run client\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false yarn install --prefix client"
  },
  "dependencies": {
    "cookie-parser": "^1.4.5",
    "debug": "^2.6.9",
    "express": "^4.16.4",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}